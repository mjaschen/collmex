# The .env file is only required when running the integration tests locally.
# In CI the variables are set via workflow secrets.
COLLMEX_USER=your_username
COLLMEX_PASSWORD=your_password
COLLMEX_CUSTOMER=your_customer_id
